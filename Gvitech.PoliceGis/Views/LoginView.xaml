<Window x:Class="MMC.MSpace.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MMC.MSpace.Views"
        mc:Ignorable="d"
        Title="{DynamicResource Login}" Height="515" Width="798"   MinHeight="515" MinWidth="798" MaxHeight="515" MaxWidth="798" WindowStartupLocation="CenterScreen" WindowState="Normal" WindowStyle="ToolWindow" Topmost="True">
    <Grid>
        <Grid.Resources>
            <Style TargetType="ScrollViewer">
                <Setter Property="HorizontalScrollBarVisibility" Value="Hidden"/>
                <Setter Property="VerticalScrollBarVisibility" Value="Hidden"/>
            </Style>
        </Grid.Resources>
        <WebBrowser Name="webBrowser" Height="480" Width="800"
                    LoadCompleted="webCtrl_LoadCompleted">
 
        </WebBrowser>
    </Grid>
</Window>
