<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:Mmc.Mspace.Theme.Controls">
    <Style x:Key="glyphicon" TargetType="{x:Type Path}">
        <Setter Property="Width" Value="16"></Setter>
        <Setter Property="Height" Value="16"></Setter>
        <Setter Property="Fill" Value="{DynamicResource Path16_N}"></Setter>
        <Setter Property="Stretch" Value="Uniform"></Setter>
    </Style>
    <Style x:Key="glyphicon6" TargetType="{x:Type Path}">
        <Setter Property="Fill" Value="{DynamicResource Path16_N}"></Setter>
        <Setter Property="Stretch" Value="Uniform"></Setter>
    </Style>
    <Style TargetType="{x:Type Button}" x:Key="BaseButton">
        <Setter Property="FontFamily" Value="Microsoft YaHei"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="#ffffff"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Cursor" Value="Hand" />
    </Style> 
    <Style TargetType="{x:Type Button}" x:Key="DefaultuploadButtonStyle" BasedOn="{StaticResource BaseButton}">
        <Setter Property="FontSize" Value="14px"/>
        <Setter Property="Height" Value="24"/>
        <Setter Property="Width" Value="84"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Padding" Value="2,1"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Background" Value="{StaticResource uploadBackground}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource DefaultButtonForeground}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}" >
                    <Border x:Name="Border" Padding="{TemplateBinding Padding}" Width="{TemplateBinding Width}" CornerRadius="2" Height="{TemplateBinding Height}" Background="{TemplateBinding Background}"  BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center"  Margin="{TemplateBinding Padding}"  VerticalAlignment="Center" Content="{TemplateBinding Content}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource DefaultButtonMouseOver}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource DefaultButtonPressed}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border" Property="BorderThickness" Value="0"/>
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource DefaultButtonIsEnabled}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="{x:Type Button}" x:Key="DefaultButtonStyle" BasedOn="{StaticResource BaseButton}">
        <Setter Property="FontSize" Value="14px"/>
        <Setter Property="Height" Value="24"/>
        <Setter Property="Width" Value="84"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Padding" Value="2,1"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Background" Value="{StaticResource DefaultButtonBackground}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource DefaultButtonForeground}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}" >
                    <Border x:Name="Border" Padding="{TemplateBinding Padding}" Width="{TemplateBinding Width}" CornerRadius="2" Height="{TemplateBinding Height}" Background="{TemplateBinding Background}"  BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center"  Margin="{TemplateBinding Padding}"  VerticalAlignment="Center" Content="{TemplateBinding Content}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource DefaultButtonMouseOver}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource DefaultButtonPressed}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                           <Setter TargetName="Border" Property="BorderThickness" Value="0"/>
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource DefaultButtonIsEnabled}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="{x:Type Button}" x:Key="DefaultOpacityButtonStyle" BasedOn="{StaticResource BaseButton}">
        <Setter Property="FontSize" Value="14px"/>
        <Setter Property="Height" Value="30"/>
        <Setter Property="Width" Value="80"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Padding" Value="2,1"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Background" Value="{StaticResource DefaultOpacityButtonBackground}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource DefaultOpacityButtonForeground}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}" >
                    <Border x:Name="Border" Padding="{TemplateBinding Padding}" Width="{TemplateBinding Width}" CornerRadius="2" Height="{TemplateBinding Height}" Background="{TemplateBinding Background}"  BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center"  Margin="{TemplateBinding Padding}"  VerticalAlignment="Center" Content="{TemplateBinding Content}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource DefaultOpacityButtonMouseOver}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource DefaultOpacityButtonPressed}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border" Property="BorderThickness" Value="0"/>
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource DefaultOpacityButtonIsEnabled}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!--清空按键的样式-->
    <Style x:Key="btnPath_clear" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="FontFamily" Value="{DynamicResource DefaultFont}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Padding" Value="1"/>
        <Setter Property="Height" Value="28"/>
        <Setter Property="Width" Value="28"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="PART_Chrome" Padding="3" Opacity="0.3" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}" SnapsToDevicePixels="true">
                        <Path x:Name="PART_Delete" Data="{StaticResource glyphicon-clear}" 
                              Fill="#fefefe" Width="16" Height="16" Stretch="Uniform"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" 
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_Delete" Property="Fill" Value="#fefefe"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="PART_Delete" Property="Fill" Value="#fefefe"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="#ADADAD"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key ="ButtonCloseStyle" BasedOn="{x:Null}" TargetType ="{x:Type Button }">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Height" Value="36"/>
        <Setter Property="Width" Value="36"/>
        <Setter Property="ToolTip" Value="{DynamicResource Shutdown}"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property ="Template">
            <Setter.Value >
                <ControlTemplate TargetType ="{ x:Type Button }">
                    <Grid x:Name ="buttonClose">
                        <Border BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="0" CornerRadius="1" x:Name="PART_Background" Background="{TemplateBinding Background}"/>


                        <Path x:Name ="iconX" Width ="12.50003" Height ="14.50006" Stretch ="Fill" Fill ="#EBF5FF" Data ="F1 M 401.281,299.818L 398.795,297.333L 401.281,294.847C 401.684,294.445 401.684,293.79 401.281,293.386C 400.877,292.982 400.222,292.982 399.82,293.386L 397.334,295.872L 394.848,293.386C 394.445,292.982 393.79,292.982 393.386,293.386C 392.982,293.79 392.982,294.445 393.386,294.848L 395.872,297.334L 393.387,299.818C 392.984,300.222 392.982,300.876 393.387,301.281C 393.791,301.684 394.445,301.683 394.848,301.279L 397.333,298.795L 399.818,301.281C 400.222,301.684 400.877,301.684 401.281,301.281C 401.684,300.877 401.684,300.222 401.281,299.818 Z "/>
                        <ContentPresenter SnapsToDevicePixels ="{ TemplateBinding SnapsToDevicePixels }" HorizontalAlignment ="{ TemplateBinding HorizontalContentAlignment }" VerticalAlignment ="{ TemplateBinding VerticalContentAlignment }" RecognizesAccessKey ="True"/>
                    </Grid>
                    <ControlTemplate.Triggers >
                        <Trigger Property ="IsFocused" Value ="True"/>
                        <Trigger Property ="IsDefaulted" Value ="True"/>
                        <Trigger Property ="IsEnabled" Value ="False"/>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Background" Value="#C75050" TargetName="PART_Background" />
                            <Setter Property="Opacity" Value="1" TargetName="PART_Background"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderThickness" TargetName="PART_Background" Value="0"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter Property="Background" Value="#C75050" TargetName="PART_Background" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key ="ButtonFoldStyle" BasedOn="{x:Null}" TargetType ="{x:Type Button }">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Height" Value="36"/>
        <Setter Property="Width" Value="36"/>
        <Setter Property="ToolTip" Value="折叠"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property ="Template">
            <Setter.Value >
                <ControlTemplate TargetType ="{ x:Type Button }">
                    <Grid x:Name ="buttonFold">
                        <Border BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="0" CornerRadius="1" x:Name="PART_Background" Background="{TemplateBinding Background}"/>
                        <Path x:Name ="iconX" Width ="12.50003" Height ="14.50006" Stretch ="Fill" Fill ="#EBF5FF" Data ="M7.4975,12 C7.76201764,12 8.01566847,12.1052329 8.2025,12.2924856 L8.2025,12.2924856 L12.0825,16.1724856 L15.9625,12.2924856 C16.3525,11.9024856 16.9925,11.9124856 17.3825,12.2924856 C17.7725,12.6824856 17.7725,13.3124856 17.3825,13.7024856 L17.3825,13.7024856 L12.7925,18.2924856 C12.4025,18.6824856 11.7725,18.6824856 11.3825,18.2924856 L11.3825,18.2924856 L6.7925,13.7024856 C6.4025,13.3124856 6.4025,12.6824856 6.7925,12.2924856 C6.97933153,12.1052329 7.23298236,12 7.4975,12 Z M7.4975,6 C7.76201764,6 8.01566847,6.10523293 8.2025,6.29248561 L8.2025,6.29248561 L12.0825,10.1724856 L15.9625,6.29248561 C16.3525,5.90248561 16.9925,5.91248561 17.3825,6.29248561 C17.7725,6.68248561 17.7725,7.31248561 17.3825,7.70248561 L17.3825,7.70248561 L12.7925,12.2924856 C12.4025,12.6824856 11.7725,12.6824856 11.3825,12.2924856 L11.3825,12.2924856 L6.7925,7.70248561 C6.4025,7.31248561 6.4025,6.68248561 6.7925,6.29248561 C6.97933153,6.10523293 7.23298236,6 7.4975,6 Z"/>
                        <ContentPresenter SnapsToDevicePixels ="{ TemplateBinding SnapsToDevicePixels }" HorizontalAlignment ="{ TemplateBinding HorizontalContentAlignment }" VerticalAlignment ="{ TemplateBinding VerticalContentAlignment }" RecognizesAccessKey ="True"/>
                    </Grid>
                    <ControlTemplate.Triggers >
                        <Trigger Property ="IsFocused" Value ="True"/>
                        <Trigger Property ="IsDefaulted" Value ="True"/>
                        <Trigger Property ="IsEnabled" Value ="False"/>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Background" Value="black" TargetName="PART_Background" />
                            <Setter Property="Opacity" Value="1" TargetName="PART_Background"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderThickness" TargetName="PART_Background" Value="0"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter Property="Background" Value="black" TargetName="PART_Background" />
                            <Setter Property="Data" Value="M11.5266667,16.0974976 C11.7911843,16.0974976 12.0448351,16.2027305 12.2316667,16.3899832 L12.2316667,16.3899832 L16.1116667,20.2699832 L19.9916667,16.3899832 C20.3816667,15.9999832 21.0216667,16.0099832 21.4116667,16.3899832 C21.8016667,16.7799832 21.8016667,17.4099832 21.4116667,17.7999832 L21.4116667,17.7999832 L16.8216667,22.3899832 C16.4316667,22.7799832 15.8016667,22.7799832 15.4116667,22.3899832 L15.4116667,22.3899832 L10.8216667,17.7999832 C10.4316667,17.4099832 10.4316667,16.7799832 10.8216667,16.3899832 C11.0084982,16.2027305 11.262149,16.0974976 11.5266667,16.0974976 Z M11.5266667,10.0974976 C11.7911843,10.0974976 12.0448351,10.2027305 12.2316667,10.3899832 L12.2316667,10.3899832 L16.1116667,14.2699832 L19.9916667,10.3899832 C20.3816667,9.99998321 21.0216667,10.0099832 21.4116667,10.3899832 C21.8016667,10.7799832 21.8016667,11.4099832 21.4116667,11.7999832 L21.4116667,11.7999832 L16.8216667,16.3899832 C16.4316667,16.7799832 15.8016667,16.7799832 15.4116667,16.3899832 L15.4116667,16.3899832 L10.8216667,11.7999832 C10.4316667,11.4099832 10.4316667,10.7799832 10.8216667,10.3899832 C11.0084982,10.2027305 11.262149,10.0974976 11.5266667,10.0974976 Z" TargetName="iconX" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key ="ButtonRefreshStyle" BasedOn="{x:Null}" TargetType ="{x:Type Button }">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Height" Value="36"/>
        <Setter Property="Width" Value="36"/>
        <Setter Property="ToolTip" Value="折叠"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property ="Template">
            <Setter.Value >
                <ControlTemplate TargetType ="{ x:Type Button }">
                    <Grid x:Name ="buttonFold">
                        <Border BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="0" CornerRadius="1" x:Name="PART_Background" Background="{TemplateBinding Background}"/>
                        <Path x:Name ="iconX" Width ="12.50003" Height ="14.50006" Stretch ="Fill" Fill ="#EBF5FF" Data ="M13.0477619,12.4725102 C10.5911496,14.9291225 6.36596307,15.1536344 3.90935084,12.6970437 C3.15994489,11.9475926 2.57247975,11.0378701 2.35108945,10.0994079 C2.35108945,10.0994079 2.16135793,9.331072 2.0580272,8.63095557 L0,8.63095557 C0.0589420217,9.12834735 0.118280001,9.61885769 0.215125446,10.0994959 C0.215125446,10.0994959 0.331932476,10.6854658 0.429678332,10.9730516 C0.462831984,11.0847581 0.499942847,11.1951896 0.536152225,11.3060813 C1.03174147,12.8235159 2.30969159,14.0636041 2.30969159,14.0636041 C2.34768181,14.1031113 2.38608991,14.1424207 2.42502544,14.1813564 C5.70528512,17.4615731 11.3050221,17.2248795 14.5853033,13.9445768 L16.5451231,15.6731794 L16.5884559,10.5389235 L11.1117171,10.5199943 L13.0477494,12.4724922 L13.0477619,12.4725102 Z M3.57728795,4.11600246 C6.03422245,1.65906796 10.3583447,1.33415938 12.8152792,3.79110677 C13.5646636,4.54053635 14.0919338,5.43887001 14.3735406,6.38859221 C14.3735406,6.38859221 14.6411223,7.55921272 14.7334365,8.63096201 L16.8087006,8.63096201 C16.731291,7.32618862 16.560356,6.23781469 16.2397035,5.20942092 C15.7059042,3.49739569 14.181831,2.19148577 14.181831,2.19148577 C10.8933428,-0.97290854 5.33425425,-0.609671789 2.09294579,2.63167105 L0.214010405,0.814997447 L0.204776646,6.02382577 L5.26415949,6.03637928 L3.57728604,4.11602412 L3.57728795,4.11600246 Z"/>
                        <ContentPresenter SnapsToDevicePixels ="{ TemplateBinding SnapsToDevicePixels }" HorizontalAlignment ="{ TemplateBinding HorizontalContentAlignment }" VerticalAlignment ="{ TemplateBinding VerticalContentAlignment }" RecognizesAccessKey ="True"/>
                    </Grid>
                    <ControlTemplate.Triggers >
                        <Trigger Property ="IsFocused" Value ="True"/>
                        <Trigger Property ="IsDefaulted" Value ="True"/>
                        <Trigger Property ="IsEnabled" Value ="False"/>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Background" Value="black" TargetName="PART_Background" />
                            <Setter Property="Opacity" Value="1" TargetName="PART_Background"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderThickness" TargetName="PART_Background" Value="0"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter Property="Background" Value="black" TargetName="PART_Background" />
                            <!--<Setter Property="Data" Value="#292C35" TargetName="iconX" />-->
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="CityMenuStyle" TargetType="{x:Type controls:Buttons}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="MouseOverBackground" Value="Transparent" />
        <Setter Property="PressedBackground" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Width" Value="Auto" />
        <Setter Property="FontSize" Value="14px" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Buttons}">
                    <Border x:Name="PART_Background" BorderBrush="{TemplateBinding BorderBrush}" Padding="{TemplateBinding Padding}" BorderThickness="{TemplateBinding BorderThickness}" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}" Background="{TemplateBinding Background}">
                        <Grid Height="{TemplateBinding Height}" Width="{TemplateBinding Width}">
                            <Image x:Name="PART_PData" Visibility="Collapsed"  Stretch="Fill" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"  Source="{Binding RelativeSource={RelativeSource TemplatedParent},Path=RIcon}"/>
                            <TextBlock x:Name="PART_Pcontent" Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Content}" ></TextBlock>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasPathData" Value="true">
                            <Setter TargetName="PART_PData" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Background" Value="{StaticResource DefaultButtonIsEnabled}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Padding" Value="0" />
        <Setter Property="Content" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>

    <Style x:Key="HomeMenuStyle" TargetType="{x:Type controls:Buttons}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="MouseOverBackground" Value="Transparent" />
        <Setter Property="PressedBackground" Value="Transparent" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Width" Value="Auto" />
        <Setter Property="FontSize" Value="14px" />
        <Setter Property="Foreground" Value="#B3BBC5" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Buttons}">
                    <Border x:Name="PART_Background" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}"  BorderBrush="{TemplateBinding BorderBrush}" Padding="{TemplateBinding Padding}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                        <Grid VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Image x:Name="PART_PData" Grid.Column="0" Visibility="Collapsed" Margin="10,0,0,0" Stretch="Uniform" Height="21" Width="22" HorizontalAlignment="Left" VerticalAlignment="Center" Source="{Binding RelativeSource={RelativeSource TemplatedParent},Path=RIcon}"/>
                            <TextBlock x:Name="PART_Pcontent" Grid.Column="1" Margin="10,0,0,0" Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Left" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Content}" ></TextBlock>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasPathData" Value="true">
                            <Setter TargetName="PART_PData" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#ffffff"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#50ABFF"/>
                            <Setter Property="Background" Value="#3E4A64"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsPressed" Value="false"/>
                                <Condition Property="IsSelected" Value="true"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.Setters>
                                <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                                <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#ffffff"/>
                                <Setter Property="Background" Value="#3E4A64"/>
                            </MultiTrigger.Setters>

                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Background" Value="{StaticResource DefaultButtonIsEnabled}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Padding" Value="0" />
        <Setter Property="Content" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>

    <Style x:Key="ComboBoxToggleButtonStyle" TargetType="{x:Type ToggleButton}">
        <Setter Property="OverridesDefaultStyle" Value="true"/>
        <Setter Property="IsTabStop" Value="false"/>
        <Setter Property="Focusable" Value="false"/>
        <Setter Property="ClickMode" Value="Press"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Grid x:Name="Grid">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Border Background="{TemplateBinding Background}" x:Name="Bg" Grid.ColumnSpan="2" Margin="0,1,1,1"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"  Opacity="0"/>
                        <Image x:Name="Image" Grid.Column="1"  Width="11" Source="{StaticResource xiala}" Stretch="Uniform" VerticalAlignment="Center" Margin="7"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <!--<Setter Property="Fill" TargetName="Image" Value="#FFFFFF" />-->
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <!--<Setter Property="Fill" TargetName="Image" Value="#207ad5" />-->
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Image" Property="LayoutTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="180" CenterX="6" CenterY="6"></RotateTransform>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Opacity" TargetName="Image" Value="0.8" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="HomeTopButtonStyle" TargetType="{x:Type controls:Buttons}">
        <Setter Property="Margin" Value="0,0"/>
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="#ffffff"/>
        <Setter Property="MouseOverBackground" Value="Transparent" />
        <Setter Property="PressedBackground" Value="Transparent" />
        <Setter Property="MouseOverForeground" Value="#0080ff" />
        <Setter Property="PressedForeground" Value="#0080ff" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="HasPathData" Value="False" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Width" Value="Auto" />
        <Setter Property="FontSize" Value="14px" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Buttons}">
                    <Border x:Name="PART_Background" BorderBrush="{TemplateBinding BorderBrush}" Padding="{TemplateBinding Padding}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                        <Grid VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Image  x:Name="PART_PData" Visibility="Collapsed" Margin="0,0" Width="32" Height="32" Stretch="Uniform" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"  Source="{Binding RelativeSource={RelativeSource TemplatedParent},Path=RIcon}"/>
                            <TextBlock x:Name="PART_Pcontent" Grid.Column="1"  Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Content}" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasPathData" Value="true">
                            <Setter TargetName="PART_PData" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource Self},Path=MouseOverForeground}"/>
                            <Setter Property="Background" Value="{Binding RelativeSource={RelativeSource Self}, Path=MouseOverBackground}"/>
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{Binding RelativeSource={RelativeSource Self},Path=PressedBackground}" />
                            <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource Self},Path=PressedForeground}"/>
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{Binding RelativeSource={RelativeSource Self},Path=PressedBackground}" />
                            <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource Self},Path=PressedForeground}"/>
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Background" Value="{DynamicResource DefaultButtonIsEnabled}"/>
                            <Setter Property="Foreground" Value="{StaticResource DefaultButtonForeground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Padding" Value="0" />
        <Setter Property="Content" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>

    <Style x:Key="HomeLeftButtonStyle" TargetType="{x:Type controls:Buttons}">
        <Setter Property="Margin" Value="0,0"/>
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="#ffffff"/>
        <Setter Property="MouseOverBackground" Value="Transparent" />
        <Setter Property="PressedBackground" Value="Transparent" />
        <Setter Property="MouseOverForeground" Value="#0080ff" />
        <Setter Property="PressedForeground" Value="#0080ff" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="HasPathData" Value="False" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Width" Value="Auto" />
        <Setter Property="FontSize" Value="14px" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Buttons}">
                    <Border x:Name="PART_Background" BorderBrush="{TemplateBinding BorderBrush}" Padding="{TemplateBinding Padding}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                        <Grid VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Image  x:Name="PART_PData" Visibility="Visible"  Margin="0,0"  Stretch="Uniform" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"  Source="{Binding RelativeSource={RelativeSource TemplatedParent},Path=RIcon}"/>
                            <TextBlock x:Name="PART_Pcontent" Grid.Column="1"  Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Content}" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasPathData" Value="true">
                            <Setter TargetName="PART_PData" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource Self},Path=MouseOverForeground}"/>
                            <Setter Property="Background" Value="{Binding RelativeSource={RelativeSource Self}, Path=MouseOverBackground}"/>
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{Binding RelativeSource={RelativeSource Self},Path=PressedBackground}" />
                            <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource Self},Path=PressedForeground}"/>
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{Binding RelativeSource={RelativeSource Self},Path=PressedBackground}" />
                            <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource Self},Path=PressedForeground}"/>
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Background" Value="{DynamicResource DefaultButtonIsEnabled}"/>
                            <Setter Property="Foreground" Value="{StaticResource DefaultButtonForeground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Padding" Value="0" />
        <Setter Property="Content" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>


    <Style x:Key="PrePointStyle" TargetType="{x:Type Button}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Image Name="btnbg" Source="{StaticResource PrePoint}"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Source"  Value="{StaticResource PrePoint_H}" TargetName="btnbg"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="NextPointStyle" TargetType="{x:Type Button}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Image Name="btnbg" Source="{StaticResource NextPoint}"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Source"  Value="{StaticResource NextPoint_H}" TargetName="btnbg"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="DeletePointStyle" TargetType="{x:Type Button}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Image Name="btnbg" Source="{StaticResource DeletePoint}"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Source"  Value="{StaticResource DeletePoint_H}" TargetName="btnbg"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
	<Style x:Key="DeletePointStyleNew" TargetType="{x:Type Button}">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type Button}">
					<Image Name="btnbg" Source="{StaticResource DeletePointNew}"/>
					<ControlTemplate.Triggers>
						<Trigger Property="IsMouseOver" Value="True">
							<Setter Property="Source"  Value="{StaticResource DeletePointNew_H}" TargetName="btnbg"/>
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
	<Style x:Key="SavePointStyle" TargetType="{x:Type Button}">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type Button}">
					<Image Name="btnbg" Source="{StaticResource SavePoint}"/>
					<ControlTemplate.Triggers>
						<Trigger Property="IsMouseOver" Value="True">
							<Setter Property="Source"  Value="{StaticResource SavePoint_H}" TargetName="btnbg"/>
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
	<Style x:Key="AddPointStyle" TargetType="{x:Type Button}">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type Button}">
					<Image Name="btnbg" Source="{StaticResource AddPoint}"/>
					<ControlTemplate.Triggers>
						<Trigger Property="IsMouseOver" Value="True">
							<Setter Property="Source"  Value="{StaticResource AddPoint_H}" TargetName="btnbg"/>
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
	<Style x:Key="ResetPointStyle" TargetType="{x:Type Button}">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type Button}">
					<Image Name="btnbg" Source="{StaticResource ResetPoint}"/>
					<ControlTemplate.Triggers>
						<Trigger Property="IsMouseOver" Value="True">
							<Setter Property="Source"  Value="{StaticResource ResetPoint_H}" TargetName="btnbg"/>
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
	<Style TargetType="{x:Type Button}" x:Key="RouteEditButtonStyle" BasedOn="{StaticResource BaseButton}">
        <Setter Property="FontSize" Value="14px"/>
        <Setter Property="Height" Value="36"/>
        <Setter Property="Width" Value="80"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Padding" Value="2,1"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Background" Value="#292C35"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource DefaultButtonForeground}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}" >
                    <Border x:Name="Border" Padding="{TemplateBinding Padding}" Width="{TemplateBinding Width}" CornerRadius="2" Height="{TemplateBinding Height}" Background="{TemplateBinding Background}"  BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center"  Margin="{TemplateBinding Padding}"  VerticalAlignment="Center" Content="{TemplateBinding Content}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#14151A"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#14151A"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border" Property="BorderThickness" Value="0"/>
                            <Setter TargetName="Border" Property="Background" Value="#14151A"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MapToggleButtonStyle" TargetType="{x:Type ToggleButton}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Foreground" Value="{DynamicResource DefaultForeground}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Grid x:Name="Grid">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Border Background="{TemplateBinding Background}" x:Name="Bg" Grid.ColumnSpan="2" Margin="0,1,1,1"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        <TextBlock x:Name="PART_Pcontent" Grid.Column="0"  Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Content}" />
                        <Image x:Name="Image" Grid.Column="1"  Width="20" Height="20" Source="{DynamicResource closecheck_P}" Stretch="Uniform" VerticalAlignment="Center"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <!--<Setter Property="Fill" TargetName="Image" Value="#FFFFFF" />-->
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <!--<Setter Property="Fill" TargetName="Image" Value="#207ad5" />-->
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Image" Property="Source" Value="{DynamicResource closecheck_N}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Opacity" TargetName="Image" Value="0.8" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="LeftWindowStyle" TargetType="{x:Type ToggleButton}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Grid x:Name="Grid">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Background="{TemplateBinding Background}" x:Name="Bg" Grid.ColumnSpan="2" Margin="0,1,1,1"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        <Image x:Name="Image" Grid.Column="0"  Width="20" Height="64" Source="{StaticResource leftdown_n}" Stretch="Uniform" VerticalAlignment="Center"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Image" Property="Source" Value="{DynamicResource leftup_n}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="True"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Source" TargetName="Image" Value="{DynamicResource leftup_f}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="True"/>
                                <Condition Property="IsPressed" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Source" TargetName="Image" Value="{DynamicResource leftup_p}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="False"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Source" TargetName="Image" Value="{DynamicResource leftdown_f}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="False"/>
                                <Condition Property="IsPressed" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Source" TargetName="Image" Value="{DynamicResource leftdown_p}" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--16px-->
    <SolidColorBrush x:Key="Path16_N" Color="#2ECC71"/>
    <SolidColorBrush x:Key="Path16_F" Color="#36ea82"/>
    <SolidColorBrush x:Key="Path16_P" Color="#207ad5"/>
    <SolidColorBrush x:Key="Path16_D" Color="#FFFFFF"/>

    <!--通用按钮样式Icon_Path  16*16-->
    <Style x:Key="Button_plusStyle" TargetType="{x:Type controls:Buttons}">
        <Setter Property="Margin" Value="10,0"/>
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="#ffffff"/>
        <Setter Property="MouseOverBackground" Value="Transparent" />
        <Setter Property="PressedBackground" Value="Transparent" />
        <Setter Property="MouseOverForeground" Value="#0080ff" />
        <Setter Property="PressedForeground" Value="#0080ff" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="HasPathData" Value="False" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Width" Value="Auto" />
        <Setter Property="FontSize" Value="14px" />
        <Setter Property="Padding" Value="1" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Buttons}">
                    <Border x:Name="PART_Background" BorderBrush="{TemplateBinding BorderBrush}" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                        <Grid  Margin="{TemplateBinding Padding}" VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Path  x:Name="PART_PData" Visibility="Collapsed" Margin="5,0,5,0" Style="{StaticResource glyphicon}" VerticalAlignment="Center" HorizontalAlignment="Left"  Data="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PathData}"/>
                            <TextBlock x:Name="PART_Pcontent" Grid.Column="1"  Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Content}" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasPathData" Value="true">
                            <Setter TargetName="PART_PData" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="PART_PData" Property="Fill" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=InnerPathFill}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource Self},Path=MouseOverForeground}"/>
                            <Setter Property="Background" Value="{Binding RelativeSource={RelativeSource Self}, Path=MouseOverBackground}"/>
                            <Setter TargetName="PART_PData" Property="Fill" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=OverInnerPathFill}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{Binding RelativeSource={RelativeSource Self},Path=PressedBackground}" />
                            <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource Self},Path=PressedForeground}"/>
                            <Setter TargetName="PART_PData" Property="Fill" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressInnerPathFill}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter TargetName="PART_PData" Property="Fill" Value="{StaticResource Path16_D}"/>
                            <Setter Property="Foreground" Value="{StaticResource Path16_D}"/>
                            <Setter Property="Opacity" Value="0.2"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Content" Value="{x:Null}" />
        <Setter Property="RIcon" Value="{x:Null}" />
        <Setter Property="MouseOverRIcon" Value="{x:Null}" />
        <Setter Property="PressedRIcon" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>


    <Style x:Key="Button_plusDefaultStyle" TargetType="{x:Type controls:Buttons}">
        <Setter Property="Margin" Value="10,0"/>
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="#ffffff"/>
        <Setter Property="MouseOverBackground" Value="Transparent" />
        <Setter Property="PressedBackground" Value="Transparent" />
        <Setter Property="MouseOverForeground" Value="#0080ff" />
        <Setter Property="PressedForeground" Value="#0080ff" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="HasPathData" Value="False" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Width" Value="Auto" />
        <Setter Property="FontSize" Value="15px" />
        <Setter Property="Padding" Value="1" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Buttons}">
                    <Border x:Name="PART_Background" BorderBrush="{TemplateBinding BorderBrush}" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                        <Grid  Margin="{TemplateBinding Padding}" VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Path  x:Name="PART_PData" Visibility="Collapsed" Height="15" Width="15" Margin="5,0,5,0" Style="{StaticResource glyphicon6}" VerticalAlignment="Center" HorizontalAlignment="Left"  Data="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PathData}"/>
                            <TextBlock x:Name="PART_Pcontent" Grid.Column="1"  Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Content}" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasPathData" Value="true">
                            <Setter TargetName="PART_PData" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="PART_PData" Property="Fill" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=InnerPathFill}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource Self},Path=MouseOverForeground}"/>
                            <Setter Property="Background" Value="{Binding RelativeSource={RelativeSource Self}, Path=MouseOverBackground}"/>
                            <Setter TargetName="PART_PData" Property="Fill" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=OverInnerPathFill}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{Binding RelativeSource={RelativeSource Self},Path=PressedBackground}" />
                            <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource Self},Path=PressedForeground}"/>
                            <Setter TargetName="PART_PData" Property="Fill" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressInnerPathFill}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter TargetName="PART_PData" Property="Fill" Value="{StaticResource Path16_D}"/>
                            <Setter Property="Foreground" Value="{StaticResource Path16_D}"/>
                            <Setter Property="Opacity" Value="0.2"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Content" Value="{x:Null}" />
        <Setter Property="RIcon" Value="{x:Null}" />
        <Setter Property="MouseOverRIcon" Value="{x:Null}" />
        <Setter Property="PressedRIcon" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>
    <Style x:Key="tagButtonStyle"  TargetType="{x:Type controls:Buttons}" BasedOn="{StaticResource Button_plusStyle}">
        <Setter Property="Foreground" Value="{DynamicResource DefaultButtonForeground}" />
        <Setter Property="InnerPathFill" Value="{StaticResource Path16_N}" />
        <Setter Property="OverInnerPathFill" Value="{StaticResource Path16_D}" />
        <Setter Property="PressInnerPathFill" Value="{StaticResource Path16_P}" />
        <Setter Property="MouseOverForeground" Value="{StaticResource Path16_D}" />
        <Setter Property="PressedForeground" Value="{StaticResource Path16_D}" />
    </Style>

    <Style x:Key="Button16Style" TargetType="{x:Type controls:Buttons}">
        <Setter Property="Margin" Value="5,0"/>
        <Setter Property="Background" Value="{DynamicResource DefaultButtonBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource DefaultButtonForeground}"/>
        <Setter Property="MouseOverBackground" Value="{DynamicResource DefaultButtonMouseOver}" />
        <Setter Property="PressedBackground" Value="{DynamicResource DefaultButtonPressed}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="HasPathData" Value="False" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Width" Value="Auto" />
        <Setter Property="FontSize" Value="14px" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Buttons}">
                    <Border x:Name="PART_Background" BorderBrush="{TemplateBinding BorderBrush}" Padding="{TemplateBinding Padding}" CornerRadius="2" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                        <Grid VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Image  x:Name="PART_PData" Visibility="Collapsed" Margin="3,0" Width="16" Height="16" Stretch="Uniform" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"  Source="{Binding RelativeSource={RelativeSource TemplatedParent},Path=RIcon}"/>
                            <TextBlock x:Name="PART_Pcontent" Margin="2,0,3,0" Grid.Column="1"  Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Content}" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasPathData" Value="true">
                            <Setter TargetName="PART_PData" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource Self},Path=MouseOverForeground}"/>
                            <Setter Property="Background" Value="{Binding RelativeSource={RelativeSource Self}, Path=MouseOverBackground}"/>
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{Binding RelativeSource={RelativeSource Self},Path=PressedBackground}" />
                            <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource Self},Path=PressedForeground}"/>
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{Binding RelativeSource={RelativeSource Self},Path=PressedBackground}" />
                            <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource Self},Path=PressedForeground}"/>
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Background" Value="{DynamicResource DefaultButtonIsEnabled}"/>
                            <Setter Property="Foreground" Value="{StaticResource DefaultButtonForeground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Padding" Value="0" />
        <Setter Property="Content" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>

    <Style x:Key="PageStyle" TargetType="{x:Type controls:Buttons}" BasedOn="{StaticResource Button16Style}">
        <Setter Property="Background" Value="{DynamicResource PageBackground}" />
        <Setter Property="MouseOverBackground" Value="{DynamicResource PageBackground}" />
        <Setter Property="PressedBackground" Value="{DynamicResource PageBackground}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Buttons}">
                    <Border x:Name="PART_Background" BorderBrush="{TemplateBinding BorderBrush}" Padding="{TemplateBinding Padding}" CornerRadius="2" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                        <Grid VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Image  x:Name="PART_PData" Visibility="Collapsed" Margin="3,0" Width="8" Height="8" Stretch="Uniform" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"  Source="{Binding RelativeSource={RelativeSource TemplatedParent},Path=RIcon}"/>
                            <TextBlock x:Name="PART_Pcontent" Margin="2,0,3,0" Grid.Column="1"  Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Content}" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasPathData" Value="true">
                            <Setter TargetName="PART_PData" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource Self},Path=MouseOverForeground}"/>
                            <Setter Property="Background" Value="{Binding RelativeSource={RelativeSource Self}, Path=MouseOverBackground}"/>
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{Binding RelativeSource={RelativeSource Self},Path=PressedBackground}" />
                            <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource Self},Path=PressedForeground}"/>
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{Binding RelativeSource={RelativeSource Self},Path=PressedBackground}" />
                            <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource Self},Path=PressedForeground}"/>
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Background" Value="{DynamicResource PageIsEnabledBackground}"/>
                            <Setter Property="Foreground" Value="{StaticResource DefaultButtonForeground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style x:Key="DateTiemButtonStyle" TargetType="{x:Type ToggleButton}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Grid x:Name="Grid">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Background="{TemplateBinding Background}" x:Name="Bg" Grid.ColumnSpan="2" Margin="0,1,1,1"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        <Image x:Name="Image" Grid.Column="0"  Width="24" Height="24" Source="{DynamicResource Datetime_n}" Stretch="Uniform" VerticalAlignment="Center"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Image" Property="Source" Value="{DynamicResource Datetime_p}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="True"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Source" TargetName="Image" Value="{DynamicResource Datetime_p}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="True"/>
                                <Condition Property="IsPressed" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Source" TargetName="Image" Value="{DynamicResource Datetime_p}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="False"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Source" TargetName="Image" Value="{DynamicResource Datetime_p}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="False"/>
                                <Condition Property="IsPressed" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Source" TargetName="Image" Value="{DynamicResource Datetime_p}" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 动画导航添加动画按钮样式-->
    <Style x:Key="NavigationAddBtnStyle" TargetType="{x:Type controls:Buttons}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="MouseOverBackground" Value="Transparent" />
        <Setter Property="PressedBackground" Value="Transparent" />
        <Setter Property="HorizontalContentAlignment" Value="Center"  />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Width" Value="Auto" />
        <Setter Property="Margin" Value="0,6,0,6"/>
        <Setter Property="FontSize" Value="14px" />
        <Setter Property="Foreground" Value="#B3BBC5" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Buttons}">
                    <Border x:Name="PART_Background" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}"  BorderBrush="{TemplateBinding BorderBrush}" Padding="{TemplateBinding Padding}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                        <Grid VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Image x:Name="PART_PData" Grid.Column="0" Visibility="Visible" Margin="10,0,0,0" Stretch="Uniform" Height="24" Width="24" HorizontalAlignment="Left" VerticalAlignment="Center" Source="{Binding RelativeSource={RelativeSource TemplatedParent},Path=RIcon}"/>
                            <TextBlock x:Name="PART_Pcontent" Grid.Column="1" Margin="4,0,0,0" Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Left" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Content}" ></TextBlock>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasPathData" Value="true">
                            <Setter TargetName="PART_PData" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#ffffff"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#50ABFF"/>

                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsPressed" Value="false"/>
                                <Condition Property="IsSelected" Value="true"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.Setters>
                                <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                                <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#ffffff"/>
                            </MultiTrigger.Setters>
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Tag}"/>
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#5D6168"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Padding" Value="0" />
        <Setter Property="Content" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>
<!-- 导入动画导航按钮样式-->
    <Style x:Key="NavigationImportBtnStyle" TargetType="{x:Type controls:Buttons}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="MouseOverBackground" Value="Transparent" />
        <Setter Property="PressedBackground" Value="Transparent" />
        <Setter Property="HorizontalContentAlignment" Value="Center"  />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Width" Value="Auto" />
        <Setter Property="FontSize" Value="14px" />
        <Setter Property="Foreground" Value="#B3BBC5" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Buttons}">
                    <Border x:Name="PART_Background" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}"  BorderBrush="{TemplateBinding BorderBrush}" Padding="{TemplateBinding Padding}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                        <Grid VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Image x:Name="PART_PData" Grid.Column="0" Visibility="Visible"  Stretch="Uniform" Height="24" Width="24" HorizontalAlignment="Left" VerticalAlignment="Center" Source="{Binding RelativeSource={RelativeSource TemplatedParent},Path=RIcon}"/>
                            <TextBlock x:Name="PART_Pcontent" Grid.Column="1" Margin="4,0,0,0" Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Left" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Content}" ></TextBlock>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasPathData" Value="true">
                            <Setter TargetName="PART_PData" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#ffffff"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#50ABFF"/>

                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsPressed" Value="false"/>
                                <Condition Property="IsSelected" Value="true"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.Setters>
                                <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                                <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#ffffff"/>
                            </MultiTrigger.Setters>
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Tag}"/>
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#5D6168"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Padding" Value="0" />
        <Setter Property="Content" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>
    <!-- 动画导航重命名按钮样式-->
    <Style x:Key="NavigationRenameBtnStyle" TargetType="{x:Type controls:Buttons}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="MouseOverBackground" Value="Transparent" />
        <Setter Property="PressedBackground" Value="Transparent" />
        <Setter Property="HorizontalContentAlignment" Value="Center"  />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Width" Value="Auto" />
        <Setter Property="FontSize" Value="14px" />
        <Setter Property="Margin" Value="8,0,4,0"/>
        <Setter Property="Foreground" Value="#B3BBC5" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Buttons}">
                    <Border x:Name="PART_Background" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}"  BorderBrush="{TemplateBinding BorderBrush}" Padding="{TemplateBinding Padding}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                        <Grid VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Image x:Name="PART_PData" Grid.Column="0" Visibility="Visible" Stretch="Uniform" Height="16" Width="16" HorizontalAlignment="Left" VerticalAlignment="Center" Source="{Binding RelativeSource={RelativeSource TemplatedParent},Path=RIcon}"/>
                            <TextBlock x:Name="PART_Pcontent" Grid.Column="1" Margin="1,0,0,0" TextWrapping="Wrap" Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Left" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" ToolTip="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Content}" Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Content}" ></TextBlock>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasPathData" Value="true">
                            <Setter TargetName="PART_PData" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#ffffff"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#50ABFF"/>
                
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsPressed" Value="false"/>
                                <Condition Property="IsSelected" Value="true"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.Setters>
                                <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                                <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#ffffff"/>

                            </MultiTrigger.Setters>

                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Tag}"/>
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#5D6168"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Padding" Value="0" />
        <Setter Property="Content" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>
<!-- 删除动画导航按钮样式-->
    <Style x:Key="NavigationRemoveBtnStyle" TargetType="{x:Type controls:Buttons}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="MouseOverBackground" Value="Transparent" />
        <Setter Property="PressedBackground" Value="Transparent" />
        <Setter Property="HorizontalContentAlignment" Value="Center"  />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Width" Value="Auto" />
        <Setter Property="Margin" Value="4,0,8,0"/>
        <Setter Property="FontSize" Value="14px" />
        <Setter Property="Foreground" Value="#B3BBC5" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Buttons}">
                    <Border x:Name="PART_Background" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}"  BorderBrush="{TemplateBinding BorderBrush}" Padding="{TemplateBinding Padding}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                        <Grid VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Image x:Name="PART_PData" Grid.Column="0" Visibility="Visible"  Stretch="Uniform" Height="16" Width="16" HorizontalAlignment="Left" VerticalAlignment="Center" Source="{Binding RelativeSource={RelativeSource TemplatedParent},Path=RIcon}"/>
                            <TextBlock x:Name="PART_Pcontent" Grid.Column="1" Foreground="#FF9F9F" TextWrapping="Wrap" HorizontalAlignment="Left" Margin="1,0,0,0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" ToolTip="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Content}" Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Content}" ></TextBlock>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasPathData" Value="true">
                            <Setter TargetName="PART_PData" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#FFFFFF"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#FC7676"/>
                            <Setter Property="Background" Value="#3E4A64"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsPressed" Value="false"/>
                                <Condition Property="IsSelected" Value="true"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.Setters>
                                <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                                <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#FC7676"/>
                                <Setter Property="Background" Value="#3E4A64"/>
                            </MultiTrigger.Setters>

                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Tag}"/>
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#5D6168"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Padding" Value="0" />
        <Setter Property="Content" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>

    <!-- 动画导航播放停止按钮样式-->
    <Style x:Key="NavigationStopBtnStyle" TargetType="{x:Type controls:Buttons}">
        <Setter Property="Margin" Value="5,0"/>
        <Setter Property="Background" Value="{DynamicResource DefaultButtonBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource DefaultButtonForeground}"/>
        <Setter Property="MouseOverBackground" Value="{DynamicResource DefaultButtonMouseOver}" />
        <Setter Property="PressedBackground" Value="{DynamicResource DefaultButtonPressed}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="HasPathData" Value="False" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="24" />
        <Setter Property="Width" Value="24" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Buttons}">
                    <Border x:Name="PART_Background">
                        <Grid VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Image  x:Name="PART_PData" Visibility="Visible"  Margin="3,0" Width="16" Height="16" Stretch="Uniform" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"  Source="{Binding RelativeSource={RelativeSource TemplatedParent},Path=RIcon}"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Tag}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DateDelBtnStyle" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid x:Name="Grid">
                        <Border>
                            <Image x:Name="Image" Width="16" Height="16" Source="{StaticResource UcDP_DateDel}" Stretch="Uniform" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Image" Property="Source" Value="{StaticResource UcDP_DateDel_H}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Image" Property="Source" Value="{StaticResource UcDP_DateDel_C}"/>
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!-- 新增台账导出台账-->
    <Style x:Key="AddandExportAccountBtnStyle" TargetType="{x:Type controls:Buttons}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="MouseOverBackground" Value="Transparent" />
        <Setter Property="PressedBackground" Value="Transparent" />
        <Setter Property="HorizontalContentAlignment" Value="Center"  />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Width" Value="Auto" />
        <Setter Property="FontSize" Value="14px" />
        <Setter Property="Foreground" Value="#B3BBC5" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Buttons}">
                    <Border x:Name="PART_Background" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}"  BorderBrush="{TemplateBinding BorderBrush}" Padding="{TemplateBinding Padding}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                        <Grid VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Image x:Name="PART_PData" Grid.Column="0" Visibility="Visible"  Stretch="Uniform" Height="22" Width="22" HorizontalAlignment="Left" VerticalAlignment="Center" Source="{Binding RelativeSource={RelativeSource TemplatedParent},Path=RIcon}"/>
                            <TextBlock x:Name="PART_Pcontent" Grid.Column="1" Margin="4,0,0,0" Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Left" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Content}" ></TextBlock>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasPathData" Value="true">
                            <Setter TargetName="PART_PData" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#ffffff"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#50ABFF"/>

                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsPressed" Value="false"/>
                                <Condition Property="IsSelected" Value="true"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.Setters>
                                <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                                <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#ffffff"/>
                            </MultiTrigger.Setters>
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Tag}"/>
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#5D6168"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Padding" Value="0" />
        <Setter Property="Content" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>

    <Style x:Key="TileLayerBtnStyle" TargetType="{x:Type controls:Buttons}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="HorizontalContentAlignment" Value="Center"  />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Width" Value="Auto" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Buttons}">
                    <Border x:Name="PART_Background" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}"  BorderBrush="{TemplateBinding BorderBrush}" Padding="{TemplateBinding Padding}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                        <Grid VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Image x:Name="PART_PData" Grid.Column="0" Visibility="Visible"  Stretch="Uniform" Height="16" Width="16" HorizontalAlignment="Left" VerticalAlignment="Center" Source="{Binding RelativeSource={RelativeSource TemplatedParent},Path=RIcon}"/>

                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasPathData" Value="true">
                            <Setter TargetName="PART_PData" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsPressed" Value="false"/>
                                <Condition Property="IsSelected" Value="true"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.Setters>
                                <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                            </MultiTrigger.Setters>
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Tag}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Padding" Value="0" />
        <Setter Property="Content" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>


    <Style x:Key="RoutePlanningLookingBtnStyle" TargetType="{x:Type controls:Buttons}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="MouseOverBackground" Value="Transparent" />
        <Setter Property="PressedBackground" Value="Transparent" />
        <Setter Property="HorizontalContentAlignment" Value="Center"  />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Width" Value="Auto" />
        <Setter Property="FontSize" Value="14px" />
        <Setter Property="Foreground" Value="#B3BBC5" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Buttons}">
                    <Border x:Name="PART_Background" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}"  BorderBrush="{TemplateBinding BorderBrush}" Padding="{TemplateBinding Padding}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                        <Grid VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="PART_Pcontent" Grid.Column="1" Margin="4,0,0,0" Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Left" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Content}" ></TextBlock>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#ffffff"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#ffffff"/>

                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsPressed" Value="false"/>
                                <Condition Property="IsSelected" Value="true"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.Setters>
                                <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#ffffff"/>
                            </MultiTrigger.Setters>
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#5D6168"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Padding" Value="0" />
        <Setter Property="Content" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>



    <Style TargetType="{x:Type Button}" x:Key="AddressSettingButtonStyle">
        <Setter Property="FontSize" Value="14px"/>
        <Setter Property="Height" Value="30"/>
        <Setter Property="Width" Value="80"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Padding" Value="2,1"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Background" Value="{StaticResource DefaultOpacityButtonBackground}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}" >
                    <Border x:Name="Border" BorderThickness="0" BorderBrush="Transparent" Padding="{TemplateBinding Padding}" Width="{TemplateBinding Width}" CornerRadius="2" Height="{TemplateBinding Height}" Background="{TemplateBinding Background}" >

                        <TextBlock x:Name="tb1" HorizontalAlignment="Center" Foreground="#50ABFF" Margin="{TemplateBinding Padding}"  VerticalAlignment="Center" Text="{TemplateBinding Content}"/>
                    </Border>
                    <!--<ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="tb1" Property="Background" Value="{StaticResource DefaultOpacityButtonMouseOver}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource DefaultOpacityButtonPressed}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border" Property="BorderThickness" Value="0"/>
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource DefaultOpacityButtonIsEnabled}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>-->
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!--地址栏新增一级地址-->
    <Style x:Key="AddressAddNewBtnStyle" TargetType="{x:Type controls:Buttons}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="MouseOverBackground" Value="Transparent" />
        <Setter Property="PressedBackground" Value="Transparent" />
        <Setter Property="HorizontalContentAlignment" Value="Center"  />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="#50ABFF"/>
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Width" Value="331" />
        <Setter Property="FontSize" Value="14px" />
        <Setter Property="Foreground" Value="#50ABFF" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Buttons}">
                    <Border x:Name="PART_Background" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}" BorderBrush="#50ABFF"
                            Padding="{TemplateBinding Padding}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                        <!--<Border.BorderBrush>
                            <DrawingBrush>
                                <DrawingBrush.Drawing>
                                    <GeometryDrawing>
                                        <GeometryDrawing.Pen>
                                            <Pen  Brush="#50ABFF" Thickness="3">
                                                <Pen.DashStyle>
                                                    <DashStyle  Dashes="2,1,2,0"/>
                                                </Pen.DashStyle>
                                            </Pen>
                                        </GeometryDrawing.Pen>
                                        <GeometryDrawing.Geometry>
                                            <RectangleGeometry Rect="0,0,160,30" />
                                        </GeometryDrawing.Geometry>
                                    </GeometryDrawing>
                                </DrawingBrush.Drawing>
                            </DrawingBrush>
                        </Border.BorderBrush>-->
                        <Grid VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <!--<Image x:Name="PART_PData" Grid.Column="0" Visibility="Visible"  Stretch="Uniform" Height="24" Width="24" HorizontalAlignment="Left" VerticalAlignment="Center" Source="{Binding RelativeSource={RelativeSource TemplatedParent},Path=RIcon}"/>-->
                            <TextBlock x:Name="PART_Pcontent"
                                       Grid.Column="1" Margin="4,0,0,0"
                                       Foreground="{TemplateBinding Foreground}"
                                       HorizontalAlignment="Left" VerticalAlignment="Center" 
                                       TextTrimming="CharacterEllipsis" 
                                       Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Content}" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasPathData" Value="true">
                            <!--<Setter TargetName="PART_PData" Property="Visibility" Value="Visible"/>-->
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <!--<Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>-->
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#B3BBC5"/>
                            <Setter TargetName="PART_Background" Property="Border.BorderBrush" Value="#B3BBC5"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <!--<Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>-->
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#ffffff"/>
                            <Setter TargetName="PART_Background" Property="Border.BorderBrush" Value="#ffffff"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsPressed" Value="false"/>
                                <Condition Property="IsSelected" Value="true"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.Setters>
                                <!--<Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>-->
                                <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#ffffff"/>
                                <Setter TargetName="PART_Background" Property="Border.BorderBrush" Value="#ffffff"/>
                            </MultiTrigger.Setters>
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <!--<Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Tag}"/>-->
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#ffffff"/>
                            <Setter TargetName="PART_Background" Property="Border.BorderBrush" Value="#ffffff"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Padding" Value="0" />
        <Setter Property="Content" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>

    <Style x:Key="AccountImageDeleteBtnStyle" TargetType="{x:Type controls:Buttons}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Width" Value="Auto" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Buttons}">
                    <Border x:Name="PART_Background" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}"  BorderBrush="{TemplateBinding BorderBrush}" Padding="{TemplateBinding Padding}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                        <Grid VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                            <Image x:Name="PART_PData" Grid.Column="0" Visibility="Visible"  Stretch="Uniform" Height="16" Width="16" HorizontalAlignment="Left" VerticalAlignment="Center" Source="{Binding RelativeSource={RelativeSource TemplatedParent},Path=RIcon}"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasPathData" Value="true">
                            <Setter TargetName="PART_PData" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Padding" Value="0" />
        <Setter Property="Content" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>

    <!--地址栏的增删设置按钮-->
    <Style x:Key="AddressBtnStyle" TargetType="{x:Type controls:Buttons}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="MouseOverBackground" Value="Transparent" />
        <Setter Property="PressedBackground" Value="Transparent" />
        <Setter Property="HorizontalContentAlignment" Value="Center"  />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Width" Value="Auto" />
        <Setter Property="FontSize" Value="14px" />
        <Setter Property="Foreground" Value="#B3BBC5" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Buttons}">
                    <Border x:Name="PART_Background" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}"  BorderBrush="{TemplateBinding BorderBrush}" Padding="{TemplateBinding Padding}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                        <Grid VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Image x:Name="PART_PData" Grid.Column="0" Visibility="Visible"  Stretch="Uniform" Height="24" Width="24" HorizontalAlignment="Left" VerticalAlignment="Center" Source="{Binding RelativeSource={RelativeSource TemplatedParent},Path=RIcon}"/>
                            <TextBlock x:Name="PART_Pcontent" Grid.Column="1" Margin="4,0,0,0" Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Left" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Content}" ></TextBlock>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasPathData" Value="true">
                            <Setter TargetName="PART_PData" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#ffffff"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#50ABFF"/>

                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsPressed" Value="false"/>
                                <Condition Property="IsSelected" Value="true"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.Setters>
                                <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                                <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#ffffff"/>
                            </MultiTrigger.Setters>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Padding" Value="0" />
        <Setter Property="Content" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>

    <Style x:Key="PoiFilterAreaBtnStyle" TargetType="{x:Type controls:Buttons}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="MouseOverBackground" Value="Transparent" />
        <Setter Property="PressedBackground" Value="Transparent" />
        <Setter Property="HorizontalContentAlignment" Value="Center"  />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Width" Value="Auto" />
        <Setter Property="FontSize" Value="14px" />
        <Setter Property="Foreground" Value="#50ABFF" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Buttons}">
                    <Border x:Name="PART_Background" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}"  BorderBrush="{TemplateBinding BorderBrush}" Padding="{TemplateBinding Padding}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                        <TextBlock x:Name="PART_Pcontent" Grid.Column="1" Margin="4,0,0,0" Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Left" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Content}" ></TextBlock>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#ffffff"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="PART_Pcontent" Property="Foreground" Value="#ffffff"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Padding" Value="0" />
        <Setter Property="Content" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>

    <Style x:Key="PoiDeleteButtonStyle" TargetType="{x:Type controls:Buttons}">
        <Setter Property="Foreground" Value="{DynamicResource DefaultButtonForeground}"/>
        <Setter Property="MouseOverBackground" Value="{DynamicResource DefaultButtonMouseOver}" />
        <Setter Property="PressedBackground" Value="{DynamicResource DefaultButtonPressed}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="HasPathData" Value="False" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="24" />
        <Setter Property="Width" Value="24" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Buttons}">
                    <Border x:Name="PART_Background" BorderBrush="{TemplateBinding BorderBrush}" Padding="{TemplateBinding Padding}"  BorderThickness="{TemplateBinding BorderThickness}" Background="Transparent" >
                        <Grid VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                            <Image  x:Name="PART_PData" Width="24" Height="24" Stretch="Uniform" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"  Source="{Binding RelativeSource={RelativeSource TemplatedParent},Path=RIcon}"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=MouseOverRIcon}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="PART_PData" Property="Source" Value="{Binding RelativeSource={RelativeSource TemplatedParent},Path=PressedRIcon}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Background" Value="{DynamicResource DefaultButtonIsEnabled}"/>
                            <Setter Property="Foreground" Value="{StaticResource DefaultButtonForeground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Padding" Value="0" />
        <Setter Property="Content" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>


    <Style x:Key="AccountImageButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="Foreground" Value="{DynamicResource DefaultButtonForeground}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate >
                    <Border x:Name="PART_Background" Width="164" Height="92" BorderBrush="{TemplateBinding BorderBrush}" Padding="{TemplateBinding Padding}"  BorderThickness="{TemplateBinding BorderThickness}" Background="Transparent" >
                        <Grid VerticalAlignment="Center" Width="164" Height="92" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                            <Image  x:Name="PART_PData" Width="164" Height="92" Stretch="Uniform" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"  Source="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Tag}"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_Background" Property="BorderBrush" Value="#50ABFF"/>
                        </Trigger>
                        <Trigger Property="Button.IsPressed" Value="True">
                            <Setter TargetName="PART_Background" Property="BorderBrush" Value="#50ABFF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AccountVideoViewBtnStyle" TargetType="{x:Type Button}">
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate >
                    <Border x:Name="PART_Background" BorderBrush="{TemplateBinding BorderBrush}" Padding="{TemplateBinding Padding}"  BorderThickness="{TemplateBinding BorderThickness}" Background="Transparent" >
                        <TextBlock x:Name="PART_Content" Foreground="#50ABFF" TextAlignment="Center" Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Content}" FontSize="14"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Button.IsMouseOver" Value="True">
                            <Setter TargetName="PART_Content" Property="Foreground" Value="#50ABFF"/>
                        </Trigger>
                        <Trigger Property="Button.IsPressed" Value="True">
                            <Setter TargetName="PART_Content" Property="Foreground" Value="#50ABFF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AccountListIsEnableBtn" TargetType="{x:Type Button}">
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate >
                    <Border x:Name="PART_Background" BorderBrush="Transparent" 
                            Padding="{TemplateBinding Padding}"  BorderThickness="{TemplateBinding BorderThickness}" Background="Transparent" >
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>